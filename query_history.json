[
  {
    "question": "Which courier has worst delivery performance?",
    "sql": "SELECT courier, \n     ROUND(AVG(actual_days - expected_days), 2) as avg_delay_days,\n     COUNT(*) as total_deliveries\n     FROM deliveries GROUP BY courier ORDER BY avg_delay_days DESC LIMIT 100;",
    "rows": 6
  },
  {
    "question": "Which states have highest cancellation rate?",
    "sql": "SELECT c.state, \n     COUNT(CASE WHEN o.status='cancelled' THEN 1 END) * 100.0 / COUNT(*) as cancellation_rate,\n     COUNT(*) as total_orders\n     FROM orders o JOIN customers c ON o.customer_id = c.customer_id\n     GROUP BY c.state ORDER BY cancellation_rate DESC LIMIT 100;",
    "rows": 13
  },
  {
    "question": "top 5 catrgories by revenue",
    "sql": "SELECT p.category, ROUND(SUM(o.amount_inr), 2) as total_revenue, COUNT(*) as orders\nFROM orders o JOIN products p ON o.product_id = p.product_id\nWHERE o.status = 'delivered'\nGROUP BY p.category ORDER BY total_revenue DESC LIMIT 5;",
    "rows": 5
  },
  {
    "question": "Which states have the highest cancellation rate?",
    "sql": "SELECT c.state, \n     COUNT(CASE WHEN o.status='cancelled' THEN 1 END) * 100.0 / COUNT(*) as cancellation_rate,\n     COUNT(*) as total_orders\n     FROM orders o JOIN customers c ON o.customer_id = c.customer_id\n     GROUP BY c.state ORDER BY cancellation_rate DESC LIMIT 100;",
    "rows": 13
  },
  {
    "question": "Which states have the highest cancellation rate?",
    "sql": "SELECT c.state, \n     COUNT(CASE WHEN o.status='cancelled' THEN 1 END) * 100.0 / COUNT(*) as cancellation_rate,\n     COUNT(*) as total_orders\n     FROM orders o JOIN customers c ON o.customer_id = c.customer_id\n     GROUP BY c.state ORDER BY cancellation_rate DESC LIMIT 100;",
    "rows": 13
  }
]